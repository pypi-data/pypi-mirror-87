from setuptools import setup

setup(name='dstdbn_probability',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['dstdbn_probability'],
      zip_safe=False)
