def {{problem_code}}(a, b, mod):
	if b == 0:
		return 1
	res = {{problem_code}}(a, b // 2, mod)
	res = res * res % mod
	if b % 2 == 1:
		res = res * a % mod
	return res


if __name__ == "__main__":
	b, p, m = map(int, input().split())
	print({{problem_code}}(b, p, m))
