[tool.poetry]
name = "graphenelibos"
version = "0.1.0"
description = ""
authors = ["graphene-project.io"]

[tool.poetry.dependencies]
python = ">=3.6"

[tool.poetry.dev-dependencies]

[tool.poetry.scripts]
graphene-sgx-sign = "graphenelibos.sgx_sign:main"
graphene-sgx-get-token = "graphenelibos.sgx_get_token:main"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
