[tool.poetry]
name = "pinger-demo"
version = "0.1.0"
description = ""
authors = ["Alex Duki <aleksey.duki.ext@nokia.com>"]

[tool.poetry.dependencies]
Pillow = { version = "^8.0", python = ">=3.6" }
pystray = "0.17.2"
python-xlib = "0.29"
six = "1.15.0"

[tool.poetry.dev-dependencies]
pytest = { version = "^5.2", python = ">=3.5" }

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
