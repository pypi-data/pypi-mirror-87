[tool.poetry]
name = "and_cli"
version = "0.4.0"
description = "Simple CLI for COMP4128"
authors = ["Felix Lonergan <felix.lonergan@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
click = "^7.1.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
pylint = "^2.6.0"
black = "^20.8b1"

[tool.poetry.scripts]
and = "and_cli.commands:_and"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
