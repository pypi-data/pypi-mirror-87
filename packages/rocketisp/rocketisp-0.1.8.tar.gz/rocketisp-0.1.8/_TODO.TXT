
*) Summary page with SI units (HTML?)

*) Add Engine Definition *.INI file (read and save with GUI)

*) Build a GUI (similar to ESP)

*) Create a Design Guide for running an analysis

*) Add conical nozzle option (effDiv)

*) Run parametric TDK with different Twall

*) Add effCham = effERE * effFFC

*) Check delta/Rt ~ (1-Cd)/2 (from LossesRealNozzleEffects.PDF)

*) Remove references to RWTD and RWTU

*) Use parametric ODK to make some fracKin plots
   (i.e. compare MLP fits to parametric values)

*) THETAI range is about 17 to 70??? (20-35)

*) effHL, add adiabatic wall flag, regen, radiation, ablative

*) Add option to set Lcham with mixing angle, Lgen, etc.

*) effDiv via MLP option is not implemented

*) Calc pcentFFC for a target Twall

*) Maybe change name of Injector object to LiqLiqInjector

*) Use Lgen to determine which propellant controls stability

*) Add effGG (about 1% per 1000 psia of Pc)

*) 

*) 




