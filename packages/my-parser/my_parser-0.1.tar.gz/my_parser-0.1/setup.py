from setuptools import setup, find_packages

setup(
	name='my_parser',
	version='0.1',
	author='aksenov',
	description='This is module for parse some data from urls',
	long_description=open('README.md').read(),
	packages=find_packages()
)
